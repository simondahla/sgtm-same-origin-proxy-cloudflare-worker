# Local development environment variables
# Copy this file to .dev.vars and update with your actual values
# .dev.vars is gitignored to keep your secrets safe

# The domain of your GTM server container
GTM_DOMAIN=measure.example.com

# The path prefix to strip from incoming requests
# e.g., if your requests come in as /xyz/gtm.js, set this to /xyz/
PATH_PREFIX=/xyz/
